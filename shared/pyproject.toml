[project]
name = "rendercv-web-schemas"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.13.5,<3"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["rendercv_web_schemas*"]
