[pytest]
# importlib import mode: worker/tests and backend/tests both contain a
# test_main.py, which the legacy "prepend" mode cannot import side by side.
addopts = --import-mode=importlib
consider_namespace_packages = true
pythonpath = worker backend
asyncio_mode = strict
testpaths = shared/tests worker/tests backend/tests
